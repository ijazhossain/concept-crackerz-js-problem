/* 
Write a function hourToMin that will take hour as the input parameter and will convert it into minutes and will return the result in minutes.
Sample Input:
2
Sample Output:
120
*/
function hourToMin(hour) {
    return hour * 60;
}
var result = hourToMin(2);
console.log(result);